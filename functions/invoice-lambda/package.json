{
  "name": "@shopcloud/invoice-lambda",
  "version": "1.0.0",
  "private": true,
  "description": "ShopCloud Invoice Lambda — async PDF invoice generation and email delivery",
  "main": "src/handler.js",
  "scripts": {
    "test": "jest --runInBand",
    "lint": "eslint 'src/**/*.js'"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0",
    "@aws-sdk/client-ses": "^3.600.0",
    "@aws-sdk/s3-request-presigner": "^3.600.0",
    "pdfkit": "^0.15.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "fast-check": "^3.19.0"
  }
}
