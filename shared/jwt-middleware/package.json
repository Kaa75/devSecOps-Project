{
  "name": "@shopcloud/jwt-middleware",
  "version": "1.0.0",
  "private": true,
  "description": "Reusable JWT validation middleware for ShopCloud services (Cognito JWKS-backed)",
  "main": "index.js",
  "scripts": {
    "test": "jest --runInBand"
  },
  "dependencies": {
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.1.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "fast-check": "^3.19.0"
  }
}
